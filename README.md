# WGan-MnistDataset
We have attempted to design a model that can generate images using CNN networks and the W-GAN algorithm. The model is implemented using the Python language and the TensorFlow library.
